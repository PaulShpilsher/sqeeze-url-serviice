import { SubmitResponse } from "./../model/submit-response";
import { SubmitRequest } from "./../model/submit-request";

export const submitUrlService = (model: SubmitRequest) => {
  return {
    shortUrlCode: "sdsd",
  };
};
